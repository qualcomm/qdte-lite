# Core dependency for qdte-lite (headless and GUI).
pylibfdt

# The GUI frontend needs PySide6 (the `qt` extra):
#   pip install "qdte-lite[qt]"

# Optional dependencies for development and testing
# Uncomment the following lines if needed:
# pympler>=1.0.1  # For memory profiling
# coverage>=7.0.0  # For code coverage testing
